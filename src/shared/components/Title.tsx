const Title = () => {
  return (
    <div className="mb-12">
      <h1 className="text-5xl font-medium mb-2">Bun + Elysia + React + Vite</h1>
      <p className="text-xl text-gray-500">Server-side rendering + Tailwind + ESLint + Prettier</p>
    </div>
  );
};

export default Title
