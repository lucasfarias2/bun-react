const Home = () => {
  return (
    <div>
      React app
      <button
        onClick={() => {
          alert();
        }}
      >
        Testtest
      </button>
    </div>
  );
};

export default Home;
